name: Build Check

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Confirm environment
        run: echo "Running in $(uname -a)"

      - name: Test script
        run: |
          echo "✅ Everything looks good!"
          exit 0
